<div class="container">
    <div class="row">
        <%= render 'users/list', user: @user,book: @book %>
        <div class="col-xs-9">
        <h2>Book detail</h2>
        <table class="table table-hover table-inverse">
            <tbody>
                <tr>
                    <th><%= attachment_image_tag @books.user, :profile_image, :fill, 60,60 %>
                    <td><%= @books.title %></td>
                    <td><%= @books.body %></td>
                </tr>
            </tbody>
        </table>
        </div>
    </div>
</div>